name: Update, audit, and deploy climate dashboard

on:
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  maintenance:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The data-source migration is being installed."
