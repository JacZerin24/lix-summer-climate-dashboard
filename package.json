{
  "name": "lix-summer-climate-dashboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "seed:data": "python scripts/unpack_seed_data.py",
    "predev": "npm run seed:data",
    "dev": "vite",
    "prebuild": "npm run seed:data",
    "build": "vite build",
    "preview": "vite preview",
    "pretest": "npm run seed:data",
    "test": "node --test",
    "update:data": "python scripts/update_live_data.py --year 2026",
    "roll:records": "python scripts/roll_forward_records.py --year 2025"
  },
  "devDependencies": {
    "vite": "^8.1.4"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}
